import sqlite3
conn = sqlite3.connect('tara_migration.py') # Creates the notebook
cursor = conn.cursor()
cursor.execute('CREATE TABLE IF NOT EXISTS mobility_logic (origin TEXT, dest TEXT, activity TEXT, requirement TEXT, priority INTEGER)')
print("✅ Notebook created!")
conn.close()